<h1>Listing pons</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @pons.each do |pon| %>
      <tr>
        <td><%= pon.name %></td>
        <td><%= link_to 'Show', pon %></td>
        <td><%= link_to 'Edit', edit_pon_path(pon) %></td>
        <td><%= link_to 'Destroy', pon, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pon', new_pon_path %>
